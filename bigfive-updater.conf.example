# /etc/bigfive-updater.conf
# ARCB Wider Updater Yapılandırma Dosyası / Configuration File
# v5.4.x "BigFive - Beacon"
#
# Bu dosyayı /etc/bigfive-updater.conf olarak kopyalayın.
# Copy this file to /etc/bigfive-updater.conf
#
# Komut satırı argümanları bu ayarları override eder.
# Command line arguments override these settings.

# ============================================
# MODLAR / MODES
# ============================================

# Varsayılan verbose modu (true/false)
# Default verbose mode
CONFIG_VERBOSE=false

# Varsayılan quiet modu (true/false)
# Default quiet mode
CONFIG_QUIET=false

# Varsayılan auto modu (true/false)
# Default auto mode (no prompts)
CONFIG_AUTO=false

# ============================================
# JSON ÇIKTI / JSON OUTPUT (v5.3+)
# ============================================

# JSON çıktı modu: none, json, json-full
# JSON output mode: none, json, json-full
# none = Normal terminal çıktısı / Normal terminal output
# json = Hafif JSON (monitoring için) / Lightweight JSON (for monitoring)
# json-full = Detaylı JSON (SIEM için) / Detailed JSON (for SIEM)
CONFIG_JSON_MODE=none

# ============================================
# BACKEND ATLAMA / SKIP BACKENDS
# ============================================

# Snapshot oluşturmayı atla (true/false)
# Skip snapshot creation (Timeshift/Snapper)
CONFIG_SKIP_SNAPSHOT=false

# Snapshot timeout (saniye) - varsayılan 300 (5 dakika)
# Snapshot timeout (seconds) - default 300 (5 minutes)
CONFIG_SNAPSHOT_TIMEOUT=300

# Flatpak güncellemelerini atla (true/false)
# Skip Flatpak updates
CONFIG_SKIP_FLATPAK=false

# Snap güncellemelerini atla (true/false)
# Skip Snap updates
CONFIG_SKIP_SNAP=false

# Firmware güncellemelerini atla (true/false)
# Skip firmware updates (fwupdmgr)
CONFIG_SKIP_FWUPD=false

# Sistem paket yöneticisini atla (true/false)
# Skip system package manager (APT/DNF/Pacman/Zypper/APK)
# Not: Bu ayar tüm sistem paket yöneticilerini etkiler
# Note: This setting affects all system package managers
CONFIG_SKIP_PKG_MANAGER=false

# ============================================
# RENKLER / COLORS (Opsiyonel / Optional)
# ============================================
# ANSI escape kodları kullanın
# Use ANSI escape codes
#
# CONFIG_COLOR_RED='\033[0;31m'
# CONFIG_COLOR_GREEN='\033[0;32m'
# CONFIG_COLOR_YELLOW='\033[0;33m'
# CONFIG_COLOR_BLUE='\033[0;34m'

# ============================================
# ÖRNEKLER / EXAMPLES
# ============================================
#
# Sadece sistem paketlerini güncelle (Flatpak, Snap, Firmware atla):
# Only update system packages (skip Flatpak, Snap, Firmware):
#   CONFIG_SKIP_FLATPAK=true
#   CONFIG_SKIP_SNAP=true
#   CONFIG_SKIP_FWUPD=true
#
# Cron için sessiz ve otomatik mod:
# Silent and automatic mode for cron:
#   CONFIG_QUIET=true
#   CONFIG_AUTO=true
#
# Snapshot'sız hızlı güncelleme:
# Quick update without snapshot:
#   CONFIG_SKIP_SNAPSHOT=true
#
# Sadece sistem güncelle, diğer her şeyi atla:
# Only system update, skip everything else:
#   CONFIG_SKIP_SNAPSHOT=true
#   CONFIG_SKIP_FLATPAK=true
#   CONFIG_SKIP_SNAP=true
#   CONFIG_SKIP_FWUPD=true

# ============================================
# DESTEKLENEN PAKET YÖNETİCİLERİ (v5.4.x BigFive)
# SUPPORTED PACKAGE MANAGERS
# ============================================
#
# APT    - Debian, Ubuntu, Zorin OS, Linux Mint
# DNF    - Fedora, RHEL, CentOS, Rocky Linux
# Pacman - Arch Linux, Manjaro, EndeavourOS
# Zypper - openSUSE Leap, openSUSE Tumbleweed
# APK    - Alpine Linux
#
# Script otomatik olarak sisteminize uygun olanı kullanır.
# The script automatically uses the appropriate one for your system.
